{
  "rules": {
    "orders": {
      ".read": true,
      "$orderId": {
        ".write": true,
        ".validate": "newData.hasChildren(['bookingId', 'name', 'phone', 'product', 'quantity', 'total', 'status', 'createdAt'])"
      }
    }
  }
}
